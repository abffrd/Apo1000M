{% extends 'base.html.twig' %}

{% block title %}Espece{% endblock %}

{% block body %}
<main class="flex justify-center items-center  ">

      <section class=" border-2 p-10  ml-2">
      <div class="px-10 md:px-12 py-10 md:py-7">
      <div class="flex items-center justify-between">
                <p tabindex="0" class="focus:outline-none text-base sm:text-lg md:text-xl lg:text-2xl font-bold leading-normal text-gray-800"> Espece </p>
                </div>
       </div>
       <!-- ************************header table*********************** -->
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ espece.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ espece.type }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn m-2" href="{{ path('app_espece_index') }}">back to list</a>

    <a class="btn m-2" href="{{ path('app_espece_edit', {'id': espece.id}) }}">edit</a>

    {# {{ include('espece/_delete_form.html.twig') }} #}
{% endblock %}
