{% extends 'base.html.twig' %}

{% block title %}Edit Adoption
{% endblock %}

{% block body %}
	<main class="flex justify-center items-center  ">

      <section class=" border-2 p-10  ml-2">
      <div class="px-10 md:px-12 py-10 md:py-7">
      <div class="flex items-center justify-between">
                <p tabindex="0" class="focus:outline-none text-base sm:text-lg md:text-xl lg:text-2xl font-bold leading-normal text-gray-800">Edit Adoption </p>
                </div>
       </div>
       <!-- ************************header table*********************** -->

	<h1> Adoptant :</h1>
      <h3>{{adoption.adoptant.prenom ?? ""}} {{adoption.adoptant.nom ?? ""}} </h3>
      <a class = "btn m-10" href="{{ path('app_adoptant_edit', {'id': adoption.adoptant.id}) }}">Saisir les coordonnées de l'adoptant</a>
	

				{{ include('adoption/_form.html.twig', {'button_label': 'Update'}) }}

				<a class = "btn m-10" href="{{ path('app_adoption_index') }}">Retour à la liste des adoptions</a>

				{#{ include('adoption/_delete_form.html.twig') }#}
			</div>

		</section>

	</main>
{% endblock %}
