{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<main class="flex justify-center items-center  h-screen">

      <section class=" border-2 p-6  ml-2">
            <div class="px-8 md:px-12 py-10 md:py-7">
                  <div class="flex items-center justify-between">
                        <p tabindex="0" class="focus:outline-none text-base sm:text-lg md:text-xl lg:text-2xl font-bold leading-normal text-gray-800">Modifier un utilisateur</p>
                  </div>
            </div>
       <!-- ************************header table*********************** -->
    

    {{ include('user/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    {# <a class= "btn" href="{{ path('app_user_index') }}">Retour</a> #}

    {#{ include('user/_delete_form.html.twig') }#}
     </div >
</section >
</main >
{% endblock %}
