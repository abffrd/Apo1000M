{# {{ form_start(form) }}
    {{ form_widget(form) }}
    
    <button class="btn m-2 ">{{ button_label|default('Enregistrer') }}</button>
    <a  href="{{ path('app_user_index') }}"><button class="btn m-20 ml-5 mr-5">Retour</button></a>
{{ form_end(form) }} #}


{{ form_start(form) }}

                {{ form_row(form.email, {'attr': {class:'rounded-md w-full :'}}) }}
                {{ form_row(form.approles, {'attr': {class:'rounded-md w-full :'}}) }}
                {{ form_row(form.password, {'attr': {class:'rounded-md w-full :'}}) }}
                {{ form_row(form.nom, {'attr': {class:'rounded-md w-full :'}}) }}
                {{ form_row(form.prenom, {'attr': {class:'rounded-md w-full :'}}) }}
                {{ form_row(form.Actif, {'attr': {class:'rounded-md w-full :'}}) }}

                {{ form_widget(form) }}

                <button class="btn m-5">{{ button_label|default('Enregistrer') }}</button>
	            <a class="btn m-10" href="{{ path('app_user_index') }}">Retour </a>

    {{ form_end(form) }}
