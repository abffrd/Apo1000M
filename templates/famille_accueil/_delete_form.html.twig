<form method="post" action="{{ path('app_famille_accueil_delete', {'id': famille_accueil.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ famille_accueil.id) }}">
    <button class="btn">Delete</button>
</form>
