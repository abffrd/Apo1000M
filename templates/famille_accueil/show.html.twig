{% extends 'base.html.twig' %}

{% block title %}FamilleAccueil{% endblock %}

{% block body %}
<main class="flex justify-center items-center ">

      <section class=" border-2 p-10  m-6">
      <div class="px-10 md:px-12 py-10 md:py-7">
      <div class="flex items-center justify-between">
                <p tabindex="0" class="focus:outline-none text-base sm:text-lg md:text-xl lg:text-2xl font-bold leading-normal text-gray-800">FamilleAccueil </p>
                </div>
       </div>
       <!-- ************************header table*********************** -->
	


		
        
    

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ famille_accueil.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ famille_accueil.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ famille_accueil.prenom }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ famille_accueil.adresse }}</td>
            </tr>
            <tr>
                <th>Code_postal</th>
                <td>{{ famille_accueil.codePostal }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ famille_accueil.ville }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ famille_accueil.commentaire }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ famille_accueil.email }}</td>
            </tr>
            <tr>
                <th>Telephone</th>
                <td>{{ famille_accueil.telephone }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn m-2" href="{{ path('app_famille_accueil_index') }}">back to list</a>

    <a class="btn m-2" href="{{ path('app_famille_accueil_edit', {'id': famille_accueil.id}) }}">edit</a>

    {#{ include('famille_accueil/_delete_form.html.twig') }#}
    
		
     </div >
</section >
</main >
{% endblock %}
